import { Component } from '@angular/core';

@Component({
  selector: 'app-header',
  // si el template estuviese aqui y no en un archivo externo, sería "template" solamente:
  templateUrl: './header.component.html',
})
export class HeaderComponent {}
